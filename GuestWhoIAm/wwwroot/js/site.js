function checkNumberOfAvaliblePages(pageIndex, pageCount) {
    if (pageIndex > pageCount) {
        alert("number is greater than the number of available subpages");
    } else {
        alert("number is greater than the number of available subpages");
    }
    
}